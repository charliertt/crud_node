<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user ? 'Editar ': 'Crear ' %> Usuario</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

<style>
    body{
        font-family: "Poppins", sans-serif;
        font-weight: 100;
        font-style: normal;
    }
</style>
</head>
<body class="bg-light">
    <div class="container py-5">
      <div class="row justify-content-center">
        <!-- Sección del título -->
        <div class="col-md-8 text-center mb-4">
          <h1 class="display-4">
            <%= user ? 'Editar ' : 'Crear ' %> Usuario
          </h1>
        </div>
      </div>
  
      <!-- Sección del formulario -->
      <div class="row justify-content-center">
        <div class="col-md-8">
          <form action="<%= user ? '/update/' + user._id : '/store' %>" method="POST" enctype="multipart/form-data" class="p-4 shadow bg-white rounded">
            
            <div class="form-group mb-3">
              <label for="name" class="form-label">Nombre:</label>
              <input type="text" id="name" name="name" class="form-control" value="<%= user ? user.name : '' %>" required>
            </div>
  
            <div class="form-group mb-3">
              <label for="email" class="form-label">Correo:</label>
              <input type="email" id="email" name="email" class="form-control" value="<%= user ? user.email : '' %>" required>
            </div>
  
            <div class="form-group mb-3">
              <label for="age" class="form-label">Edad:</label>
              <input type="number" id="age" name="age" class="form-control" value="<%= user ? user.age : '' %>" required>
            </div>
  
            <div class="form-group mb-4">
              <label for="img" class="form-label">Imagen:</label>
              <input type="file" id="img" name="img" class="form-control">
            </div>
  
            <!-- Si el usuario ya tiene imagen, mostrar previsualización -->
            <% if (user && user.img) { %>
              <div class="mb-3 text-center">
                <label>Imagen actual:</label>
                <img src="<%= user.img %>" alt="Imagen actual" class="img-thumbnail" style="max-width: 150px;">
                <p class="text-muted">Ruta: <%= user.img %></p>
              </div>
            <% } %>
  
            <div class="text-center">
              <button type="submit" class="btn btn-primary">
                <%= user ? 'Actualizar' : 'Crear' %> Usuario
              </button>
              <!-- <button class="btn btn-secondary ms-2">
                <a href="/">Ver Registros</a>
              </button> -->
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
  
</html>